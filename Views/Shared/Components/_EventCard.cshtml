@model Event
@{
    var description = Model.Description.Length > 140
        ? Model.Description[..140].TrimEnd() + "…"
        : Model.Description;
}

<article class="event-card reveal">
    <a asp-controller="Event" asp-action="Details" asp-route-id="@Model.Id" class="event-card__link">
        <div class="event-card__image" style="--card-bg: url('@Model.DisplayImagePath')"></div>
        <div class="event-card__body">
            <time class="event-card__date" datetime="@Model.EventDate.ToString("yyyy-MM-dd")">
                @Model.EventDate.ToString("d MMMM yyyy", new System.Globalization.CultureInfo("ru-RU"))
            </time>
            <h3 class="event-card__title">@Model.Title</h3>
            <p class="event-card__desc">@description</p>
            <span class="link-arrow">Подробнее</span>
        </div>
    </a>
</article>
